# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-mylayer3"
BBFILE_PATTERN_meta-mylayer3 = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-mylayer3 = "6"

LAYERDEPENDS_meta-mylayer3 = "core"
LAYERSERIES_COMPAT_meta-mylayer3 = "kirkstone"

# Switch this image to systemd so systemctl is available.
DISTRO_FEATURES:append = " systemd"
VIRTUAL-RUNTIME_init_manager = "systemd"
VIRTUAL-RUNTIME_initscripts = "systemd-compat-units"
VIRTUAL-RUNTIME_dev_manager = "udev"
